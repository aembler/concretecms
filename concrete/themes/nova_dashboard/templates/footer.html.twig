<footer class="border-t border-base-300 bg-base-100">
    <div class="mx-auto flex max-w-screen-2xl flex-col gap-3 px-4 py-4 text-xs text-base-content/40 sm:flex-row sm:items-center sm:justify-between sm:px-6 lg:px-8">

        {# Left: copyright + legal links #}
        <div class="flex items-center gap-4">
            <span>&copy; {{ "now"|date("Y") }} Nova Dashboard</span>
            <span class="h-3 w-px bg-base-300"></span>
            <a href="#" class="transition-colors hover:text-base-content/60">Privacy</a>
            <a href="#" class="transition-colors hover:text-base-content/60">Terms</a>
            <a href="#" class="transition-colors hover:text-base-content/60">Cookies</a>
        </div>

        {# Right: status + stack badges #}
        <div class="flex items-center gap-4">
            <a href="#" class="flex items-center gap-1.5 transition-colors hover:text-base-content/60">
                <span class="relative flex h-2 w-2">
                    <span class="absolute inline-flex h-full w-full animate-ping rounded-full bg-success opacity-60"></span>
                    <span class="relative inline-flex h-2 w-2 rounded-full bg-success"></span>
                </span>
                All systems operational
            </a>
            <span class="h-3 w-px bg-base-300"></span>
            <div class="flex items-center gap-1.5">
                <span class="badge badge-ghost badge-xs py-2 font-normal">Tailwind v4</span>
                <span class="badge badge-ghost badge-xs py-2 font-normal">daisyUI</span>
            </div>
        </div>

    </div>
</footer>

{% extends '@theme/templates/skeleton.html.twig' %}
{% block content %}
    {% if success %}
        <div class="container">
            <div class="row mt-5">
                <div class="col-12">
                    <div class="alert alert-success">{{ success }}</div>
                </div>
            </div>
        </div>
    {% endif %}

    {{ innerContent|raw }}
{% endblock %}